version: 0.2
phases:
  build:
    commands:
      - cd backend
      - dotnet publish -c Release -r linux-x64 --self-contained true -o ../publish_output
artifacts:
  base-directory: publish_output
  files:
    - '**/*'